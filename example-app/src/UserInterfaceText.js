export class UserInterfaceText {
  getMainMenuText() {
    return `
    ____________________________________________
    ||----------------------------------------||
    ||          RESULT CALCULATOR             ||
    ||----------------------------------------||
    ||----------------------------------------||
    || 1. Add a number to collection          ||
    || 2. List all numbers in the collection  ||
    || 3. Get detailed information (sub menu) ||
    || 4. Exit application                    ||
    ||----------------------------------------||
    `
  }
}